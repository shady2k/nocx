# This repository's rules for cass-memory.
# Merged with the global ~/.cass-memory/playbook.yaml; repo rules win.
#
# Deliberately empty as of 2026-09-05. It held 144 memories carried over from
# `bd remember` plus 3 from the first reflection run, and they were dropped rather
# than curated: 19 of them named tools that no longer exist (bd, Dolt, merge-slot,
# Orca) and 97 of 147 sat in category `general`, which made the gap analysis report
# `debugging: 1` while debugging rules were in the pile. A store that has to be
# read sceptically is worse than an empty one.
#
# Nothing is lost: the 144 are in .internal/memories-export.jsonl and the whole
# 147-rule file is in git history (see the commit that emptied this).
#
# Refill it from work, one rule at a time, by the three tests in AGENTS.md:
#   cm playbook import rules.json --repo
schema_version: 2
name: nocx-repo-playbook
description: nocx rules and lessons, each bought by a specific failure
metadata:
  createdAt: 2026-09-05T19:03:00.000Z
  totalReflections: 0
  totalSessionsProcessed: 0
deprecatedPatterns: []
bullets: []
