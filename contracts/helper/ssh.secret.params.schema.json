{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/helper/ssh.secret.params.schema.json",
  "title": "HelperSSHSecretParams",
  "description": "The helper asking the coordinator for material it has to PRESENT. This is a reverse request: it travels from the helper to the coordinator over the connection the probe arrived on, and the coordinator answers it through a handler registered at its composition root. The reference is the coordinator's own and the helper never interprets it, so a helper cannot ask for material the coordinator did not name.",
  "type": "object",
  "additionalProperties": false,
  "required": ["credentialRef", "purpose"],
  "properties": {
    "credentialRef": {
      "$ref": "https://nocx.local/contracts/helper/ssh.schema.json#/$defs/credential"
    },
    "purpose": { "$ref": "https://nocx.local/contracts/helper/ssh.schema.json#/$defs/purpose" }
  }
}
