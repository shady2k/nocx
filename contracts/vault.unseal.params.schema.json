{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/vault.unseal.params.schema.json",
  "title": "VaultUnsealParams",
  "description": "Params for vault.unseal.",
  "type": "object",
  "additionalProperties": false,
  "required": ["means"],
  "properties": {
    "means": {
      "type": "string",
      "enum": ["passphrase", "recovery"],
      "description": "How the caller wants to open the vault. There were three and there are two: no vault holds an OS-held root key any more (ADR-0050 step 1), so a means that read one could only ever fail."
    },
    "secret": {
      "type": "string",
      "maxLength": 64000,
      "description": "Passphrase or recovery material; never log or echo it."
    },
    "secretId": { "type": "string", "maxLength": 256 }
  }
}
