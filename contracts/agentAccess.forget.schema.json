{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/agentAccess.forget.schema.json",
  "title": "AgentAccessForget",
  "description": "Result of agentAccess.forget: the answer is no longer remembered, so the next time that agent is started nocx asks again. Nothing reaches into a pane already running: the admit check reads the store on every call, so an agent whose answer is gone has its next tool call refused and goes on running without nocx's tools — the same state a denial produces. There is therefore no run-timing choice here, unlike policy.forgetRule, because there is no work that could be left deciding under an answer that no longer exists.",
  "type": "object",
  "additionalProperties": false,
  "required": ["forgotten"],
  "properties": {
    "forgotten": {
      "type": "boolean",
      "description": "True when an answer was there and is now gone. False is a SUCCESS: the three facts named no remembered answer, which is the state the person asked for. Raising would turn a second click — or a page whose read predates somebody else's forget — into an error about what they wanted."
    }
  }
}
