{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/vault.renameSecret.schema.json",
  "title": "VaultRenameSecret",
  "description": "Result of the vault.renameSecret JSON-RPC method: the row (addressed by its opaque handle) was renamed on the vault's own record. The renderer reloads the inventory to see the new name.",
  "type": "object",
  "additionalProperties": false,
  "required": []
}
