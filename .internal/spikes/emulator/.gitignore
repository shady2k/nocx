# Vendored upstream source for the spike, not evidence and not shipped.
.vendor/
# Zig build output for libghostty-vt.
zig-out/
.zig-cache/
# The measurement's machine output: per capture, per emulator, per feed shape,
# plus the scores. About 12 MB, regenerated byte-identically by ./geometry.sh
# from the corpus's bytes — which ARE committed, so the corpus and the script
# are the reproducible artefact and this is not.
results/geometry/
