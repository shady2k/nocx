{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/api.request.send.schema.json",
  "title": "ApiRequestSendResult",
  "description": "Result of the api.request.send JSON-RPC method: what came back from one exchange, bounded. The body is captured behind the 2 MiB ceiling files.read already uses (design \u00a712.3), so a response larger than that arrives truncated rather than not at all.",
  "type": "object",
  "additionalProperties": false,
  "required": ["response", "environment", "route"],
  "properties": {
    "response": {
      "$ref": "#/$defs/response"
    },
    "environment": {
      "description": "The NAME of the environment this exchange actually went out under, as the environment FILE declares it \u2014 and \"\" when the send named none, which is the request as written on the direct route. It is the backend's own account rather than an echo of the caller's `envRelPath`: the renderer names an environment by its path and the name is read out of the file at the moment the address and the route are (capability.SendInputs), so this is the one field that says which record answered. A run list drawn from what the renderer BELIEVED it asked for is the vault.status defect in reverse \u2014 a value written by one side and never read back from the other.",
      "type": "string"
    },
    "route": {
      "description": "HOW this exchange got there: the route read off the same environment record the address came from (design \u00a76.5), never an echo of anything the caller sent. `direct` left from this machine; `connection` left through the named SSH profile's pooled connection \u2014 the same one a terminal tab uses, authorized the same way. A file that omits the route reads as direct, and the zero value is spelled out here rather than sent as \"\", so the renderer meets one spelling of one state. The profileId is an ID and not a name: the name belongs to whoever owns connections, and the renderer turns one into the other for display.",
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "profileId", "insecureTls"],
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["direct", "connection"]
        },
        "profileId": {
          "type": "string"
        },
        "insecureTls": {
          "description": "Send WITHOUT verifying the server's certificate \u2014 for a development host with a self-signed certificate. It is on the ROUTE because it is part of how a destination is reached, and it is per ENVIRONMENT rather than per app on purpose: a person turns it on for dev and cannot thereby turn it off for production, which is what a global switch would do the moment they forgot it was set. It is written into the collection file, so a colleague sees it in review, and every run that went out under it says so on the run \u2014 it can never be quietly on.",
          "type": "boolean"
        }
      }
    }
  },
  "$defs": {
    "response": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "status",
        "headers",
        "text",
        "binary",
        "lossy",
        "truncated",
        "size",
        "timings",
        "tlsVersion",
        "remoteAddr",
        "raw",
        "tlsCipherSuite",
        "certificates"
      ],
      "properties": {
        "status": {
          "type": "integer"
        },
        "headers": {
          "description": "Never null \u2014 a response with no headers is [].",
          "type": "array",
          "items": {
            "$ref": "api.request.read.schema.json#/$defs/header"
          }
        },
        "text": {
          "description": "The decoded body, always valid UTF-8, and EMPTY when binary: the run says \"binary body, N bytes\" and never base64.",
          "type": "string"
        },
        "binary": {
          "description": "A NUL byte among the bytes actually read \u2014 the files.read heuristic, labelled as one.",
          "type": "boolean"
        },
        "lossy": {
          "description": "The bytes read were not valid UTF-8 and invalid sequences were replaced. Distinct from binary: a NUL-free latin-1 body is lossy text, not a binary body. Three separate facts because they are three separate sentences in the run, and collapsing any two of them loses one.",
          "type": "boolean"
        },
        "truncated": {
          "description": "The ceiling was reached and one further byte was readable \u2014 the body shown is a prefix.",
          "type": "boolean"
        },
        "size": {
          "description": "Body bytes actually read and kept, which is the ceiling when truncated. Not a claim about what the server holds: a Content-Length can lie and a chunked response declares nothing.",
          "type": "integer"
        },
        "timings": {
          "$ref": "#/$defs/timings"
        },
        "tlsVersion": {
          "type": "string"
        },
        "remoteAddr": {
          "type": "string"
        },
        "raw": {
          "$ref": "#/$defs/exchange"
        },
        "tlsCipherSuite": {
          "description": "The negotiated cipher suite's name, and \"\" when the exchange was not over TLS.",
          "type": "string"
        },
        "certificates": {
          "description": "The chain the SERVER PRESENTED, leaf first \u2014 not the chain that was verified, which is a different list and is empty when verification was off. It is the answer to the only question left once an environment accepts self-signed certificates: which certificate did I actually just trust. Described by the side that saw the bytes and never sent as DER: a renderer that parsed certificates would be a second X.509 implementation, and the fingerprint \u2014 the field somebody compares against a value read out over the phone \u2014 must be computed once. Never null: a plain http exchange presents none and that is [].",
          "type": "array",
          "items": {
            "$ref": "#/$defs/certificate"
          }
        }
      }
    },
    "exchange": {
      "description": "The raw text of both sides, segmented (design \u00a711). It rides on the send result rather than on a method of its own because the raw text belongs to a PARTICULAR run \u2014 this exchange, these substitutions, this truncation \u2014 so a second round trip could only fetch the raw of a different send. The two sides are two fields because they are two mechanisms with two different guarantees (\u00a711.3): the request side verifies placements the sender itself made, the response side runs a bounded known-plaintext search.",
      "type": "object",
      "additionalProperties": false,
      "required": ["request", "response"],
      "properties": {
        "request": {
          "$ref": "#/$defs/raw"
        },
        "response": {
          "$ref": "#/$defs/raw"
        }
      }
    },
    "raw": {
      "description": "One side of the exchange, already segmented. A secret's VALUE never appears here in any state: the bytes are elided and a placeholder naming the secret takes their place, so a renderer that ignores spans entirely still shows no credential (\u00a711.2, ADR-0011).",
      "type": "object",
      "additionalProperties": false,
      "required": ["text", "spans"],
      "properties": {
        "text": {
          "description": "The text as it crosses, AFTER elision. Bounded by the same 2 MiB ceiling as the captured body.",
          "type": "string"
        },
        "spans": {
          "description": "Never null \u2014 a side with nothing to mark is []. The spans tile the text in order with neither gap nor overlap, so a renderer draws the whole payload by walking them.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/span"
          }
        }
      }
    },
    "span": {
      "description": "One run of the raw text. Three kinds, never two (\u00a711.1): the bytes still equal the secret (secret, named \u2014 the badge is evidence rather than a curtain); our span but the bytes differ (secret-damaged, naming the SHAPE of the damage and never its bytes, because a truncated token is a prefix of a live one); or not our span at all (text).",
      "type": "object",
      "additionalProperties": false,
      "required": ["from", "to", "kind", "name", "damage"],
      "properties": {
        "from": {
          "description": "Byte offset into text where this run starts.",
          "type": "integer"
        },
        "to": {
          "description": "Byte offset into text where this run ends.",
          "type": "integer"
        },
        "kind": {
          "description": "Which of the three states this run is.",
          "type": "string",
          "enum": ["text", "secret", "secret-damaged"]
        },
        "name": {
          "description": "The NAME of the secret, never its value. Empty for a text run.",
          "type": "string"
        },
        "damage": {
          "description": "The shape of the damage \u2014 \"truncated, 24 of 214 bytes\" \u2014 carrying only lengths, because the surviving bytes are the beginning of a live credential. Empty unless the kind is secret-damaged.",
          "type": "string"
        }
      }
    },
    "timings": {
      "description": "The phases of one exchange, in milliseconds. On a redirect chain dns, connect and tls describe the LAST hop \u2014 the one the body came from \u2014 ttfb is measured from the start of the exchange, and total adds the body read. A reused connection has a zero dns and connect, which is the honest answer: nothing was resolved and nothing was dialled.",
      "type": "object",
      "additionalProperties": false,
      "required": ["dnsMs", "connectMs", "tlsMs", "ttfbMs", "totalMs"],
      "properties": {
        "dnsMs": {
          "type": "number"
        },
        "connectMs": {
          "type": "number"
        },
        "tlsMs": {
          "type": "number"
        },
        "ttfbMs": {
          "type": "number"
        },
        "totalMs": {
          "type": "number"
        }
      }
    },
    "certificate": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "subject",
        "issuer",
        "notBefore",
        "notAfter",
        "dnsNames",
        "ipAddresses",
        "selfSigned",
        "fingerprint"
      ],
      "properties": {
        "subject": {
          "type": "string"
        },
        "issuer": {
          "type": "string"
        },
        "notBefore": {
          "description": "RFC 3339, UTC.",
          "type": "string"
        },
        "notAfter": {
          "description": "RFC 3339, UTC.",
          "type": "string"
        },
        "dnsNames": {
          "description": "The SANs, which is what a host name is actually checked against \u2014 the CN in the subject has not been the answer since 2017, and showing it alone is how somebody concludes a certificate is fine when the host is not on it. Never null.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "ipAddresses": {
          "description": "The IP SANs. Never null.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "selfSigned": {
          "description": "Subject and issuer are the same name. A description of THIS certificate and never a verdict about the connection: a self-signed leaf is exactly what an environment that accepts self-signed certificates is for.",
          "type": "boolean"
        },
        "fingerprint": {
          "description": "SHA-256 of the DER, lower-case hex in colon-separated pairs \u2014 the spelling `openssl x509 -fingerprint -sha256` prints, so the value on screen compares with the one in a terminal without either being reformatted.",
          "type": "string"
        }
      }
    }
  }
}
