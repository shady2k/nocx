{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/secrets.saveKeyMaterial.params.schema.json",
  "title": "SecretsSaveKeyMaterialParams",
  "description": "Params for secrets.saveKeyMaterial.",
  "type": "object",
  "additionalProperties": false,
  "required": ["keyText"],
  "properties": {
    "keyText": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64000,
      "description": "Private-key material to store; never log or echo it."
    },
    "name": { "type": "string", "maxLength": 200 }
  }
}
