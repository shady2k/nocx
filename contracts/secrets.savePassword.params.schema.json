{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/secrets.savePassword.params.schema.json",
  "title": "SecretsSavePasswordParams",
  "description": "Params for secrets.savePassword.",
  "type": "object",
  "additionalProperties": false,
  "required": ["password"],
  "properties": {
    "password": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64000,
      "description": "Password material to store; never log or echo it."
    },
    "name": { "type": "string", "maxLength": 200 }
  }
}
