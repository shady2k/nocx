{
  "internal/transport/ws.go:func() {": "the HTTP server's Serve loop (Start): one goroutine per server, the listener owner; the read loop invariant does not apply to it",
  "internal/transport/ws.go:s.readLoop(ctx, wconn, state, readErr)": "the per-connection read loop itself: the goroutine that owns the read-loop invariant (ADR-0026 item 1) is spawned here, not by a handler",
  "internal/transport/ws_control.go:func() {": "inflight.waitDrained's helper: waits on the WaitGroup and closes a done channel so Stop can bound the drain; a pure waiter with no store access",
  "internal/transport/ws_download.go:s.emitTransferProgress(rt)": "the running transfer's progress emitter (upload design §5.3, and the download half of nocx-9le.8.1 unchanged): exactly one per transfer in EITHER direction, started beside s.runUpload or s.runDownload and ending when that transfer's done channel closes, so it is bounded in number by maxTransfers and in time by the transfer it belongs to — the same lifetime and the same accounting as the transfer goroutine beside it, which is already baselined. It exists because the alternative is worse in both directions: the engine's copy loop calls progress once per 256 KiB chunk, so marshalling and enqueueing there would put that work on the write path thousands of times a second and fill the connection's refreshable queue (outbound.DefaultQueueDepth = 256), which does not merely drop frames but trips the stall notice the renderer treats as a cue to reconnect. This goroutine reads a single-slot mailbox instead, so a burst of chunks collapses into one notification carrying the newest byte count and at most one is ever outstanding per transfer. It escapes none of the four accountings: it holds no admission and no domain ownership, it answers no caller, every send is a non-blocking TryNotify that is dropped rather than retried, and shutdown accounts for it — WSServer.Stop calls cancelAllTransfers, and a cancelled transfer closes rt.done, which is this loop's only exit.",
  "internal/transport/ws_download.go:s.runDownload(rt, source)": "the running download (upload design D8, in the read direction): a transfer routinely outlives the call that started it — a 2 GB download over a slow link is a working download — so files.download pins the file, mints a ticket and returns. What the goroutine carries is the binding's SOURCE and the already-open handle, and nothing else: the filesystem handle and its use-guard are both let go when the RPC answers, so files.close, session teardown and shutdown cancel it, wait a bounded moment and then close for real rather than draining a guard the transfer still holds. It does NOT escape the four accountings: it carries its own context, owned by the transfer and bounded by its session; the admission it leaves is deliberate and is the decision itself; it answers no caller, so nothing is left waiting on it; and shutdown accounts for it — WSServer.Stop calls cancelAllTransfers, which cancels every transfer in both directions and waits, bounded, before the drain. Bounded in number by maxTransfers. Its one obligation beyond the upload's is the pinned handle: the deferred Close on this goroutine is the only place that descriptor is released, on every path including the one where nobody ever fetched.",
  "internal/transport/ws_files.go:h.machine.filesPollLoop(w)": "the files panel's per-connection poll loop (fm-w8): one persistent goroutine per attached connection, torn down when the connection closes; spawned by the files-binding machine, never by a control handler",
  "internal/transport/ws_files.go:s.filesBindingClosed(w.bindingID)": "fire-and-forget binding teardown when a connection drops: unregisters the binding and stops its poll loop off the disconnect path",
  "internal/transport/ws_git.go:func() {": "the git.changed notification fan-out on session close: notify every affected binding without blocking the close path; fire-and-forget notifications must not queue behind a socket write",
  "internal/transport/ws_installed_fact.go:s.resolveAndRecordInstalledFact(f.Destination.Host, f.Destination.User, f.Destination.Port, dom.BundleGeneration)": "the installed-fact write (nocx-ak2d): resolving the destination runs ssh -G as a subprocess, and PublishLifecycle is called synchronously by the publisher, so doing it inline would put a fork+exec between a shell reaching its prompt and the renderer being told. Not control-handler work — nothing is admitted, no lane is held, no domain ownership is taken and no caller is answered; the only effects are one document written and a log line. Bounded to once per domain by installedFactRecorded and in time by installedFactResolveTimeout, so neither a chatty lane nor a hung oracle accumulates goroutines.",
  "internal/transport/ws_session_handlers.go:h.sess.replayStoredForwards(cfg.ProfileID, cfg.Host, cfg.Remote)": "stored-forward replay after the open ack (spec §8 D5): runs on its own goroutine — never before the ack — so a slow connector acquire cannot delay it; connection-owned by design",
  "internal/transport/ws_session_handlers.go:h.sess.pumpToRing(context.Background(), sess, rx.ring)": "the PTY output pump: background context so the pump outlives any single WebSocket (AD-9); the ring survives the connection and the monitor owns its lifetime",
  "internal/transport/ws_session_handlers.go:h.sess.monitorExit(rx, sess)": "the per-session exit monitor: exactly one per session (monitorOnce), connection-independent like the ring it writes to",
  "internal/transport/ws_session_handlers.go:h.sess.ringToConn(ctx, wconn, sidBytes, rx.ring, 0)": "the ring-to-connection relay on attach: replays buffered output to the newly attached subscriber on its own goroutine, cancelled by the connection context",
  "internal/transport/ws_session_handlers.go:h.machine.ringToConn(ctx, wconn, sidBytes, rx.ring, from)": "the reattach relay: a second connection taking over a session starts its own replay goroutine, cancelled by its own connection context",
  "internal/transport/ws_upload.go:s.emitTransferProgress(rt)": "the running transfer's progress emitter (upload design §5.3, and the download half of nocx-9le.8.1 unchanged): exactly one per transfer in EITHER direction, started beside s.runUpload or s.runDownload and ending when that transfer's done channel closes, so it is bounded in number by maxTransfers and in time by the transfer it belongs to — the same lifetime and the same accounting as the transfer goroutine beside it, which is already baselined. It exists because the alternative is worse in both directions: the engine's copy loop calls progress once per 256 KiB chunk, so marshalling and enqueueing there would put that work on the write path thousands of times a second and fill the connection's refreshable queue (outbound.DefaultQueueDepth = 256), which does not merely drop frames but trips the stall notice the renderer treats as a cue to reconnect. This goroutine reads a single-slot mailbox instead, so a burst of chunks collapses into one notification carrying the newest byte count and at most one is ever outstanding per transfer. It escapes none of the four accountings: it holds no admission and no domain ownership, it answers no caller, every send is a non-blocking TryNotify that is dropped rather than retried, and shutdown accounts for it — WSServer.Stop calls cancelAllTransfers, and a cancelled transfer closes rt.done, which is this loop's only exit.",
  "internal/transport/ws_upload.go:s.runUpload(rt, sink)": "the running transfer (upload design D8): a transfer routinely outlives the call that started it — a 2 GB upload over a slow link is a working upload — so files.upload mints and starts and returns. What the goroutine carries is the binding's SINK and nothing else: the handle and its use-guard are both let go when the RPC answers, so files.close, session teardown and shutdown cancel it, wait a bounded moment and then close for real rather than draining a guard the transfer still holds. It does NOT escape the four accountings: it carries its own context, owned by the transfer and bounded by its session; the admission it leaves is deliberate and is the decision itself; it answers no caller, so nothing is left waiting on it; and shutdown accounts for it — WSServer.Stop calls cancelAllUploads, which cancels every transfer and waits, bounded, before the drain. Bounded in number by maxUploadTransfers.",
  "internal/transport/control/ordered.go:s.worker()": "the ordered submission's single worker: one persistent goroutine draining the FIFO (started lazily on first submit, lives for the server — the same class as an outbound pump); task N+1 never starts before task N finishes",
  "internal/transport/control/submission.go:runAndRelease(permit, ctx, task)": "the bounded submission's task runner: the ONE goroutine per admitted task, started by the scheduling contract itself after TryAcquire succeeded — the permit is released on every exit path (runAndRelease), which is exactly the accounting a handler-spawned goroutine would escape"
}
