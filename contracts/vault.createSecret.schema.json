{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/vault.createSecret.schema.json",
  "title": "VaultCreateSecret",
  "description": "Result of the vault.createSecret JSON-RPC method: the user created a secret on the Secrets page, so the name and kind were asked of them, and the value went to the default store. The renderer reloads the inventory to see the row.",
  "type": "object",
  "additionalProperties": false,
  "required": []
}
