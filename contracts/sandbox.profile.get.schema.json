{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/sandbox.profile.get.schema.json",
  "title": "SandboxProfileGet",
  "description": "Effective sandbox profile for the workspace of the pane named in the request (design 2026-08-23 §4.3). The backend resolves pane → workspace and reports either an explicit workspace profile (source=workspace) or the inherited standard profile (source=standard, inherited=true). Paths are PrivateMetadata and appear only in this explicit result.",
  "type": "object",
  "additionalProperties": false,
  "required": ["workspaceId", "source", "revision", "inherited", "writablePaths", "readOnlyPaths"],
  "properties": {
    "workspaceId": {
      "description": "The layout workspace the pane belongs to, resolved by the backend.",
      "type": "string",
      "minLength": 1
    },
    "source": {
      "description": "Which profile sourced these roots: an explicit workspace profile, or the inherited standard profile.",
      "type": "string",
      "enum": ["standard", "workspace"]
    },
    "revision": {
      "description": "The optimistic-concurrency token for the reported profile: the settings revision for a standard source, the per-workspace sandboxProfile.revision for a workspace source.",
      "type": "integer",
      "minimum": 0
    },
    "inherited": {
      "description": "Whether the workspace falls back to the standard profile rather than carrying an explicit one.",
      "type": "boolean"
    },
    "writablePaths": {
      "description": "Canonical existing directories granted read-write in addition to the workspace.",
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "readOnlyPaths": {
      "description": "Canonical existing directories granted read-only.",
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    }
  }
}
