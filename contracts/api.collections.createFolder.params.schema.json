{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/api.collections.createFolder.params.schema.json",
  "title": "APICollectionsCreateFolderParams",
  "description": "Params for api.collections.createFolder.",
  "type": "object",
  "additionalProperties": false,
  "required": ["handle", "name"],
  "properties": {
    "handle": { "type": "string", "pattern": "^[0-9a-f]{32}$" },
    "parentRelPath": { "type": "string", "maxLength": 4096 },
    "name": { "type": "string", "minLength": 1, "maxLength": 256 }
  }
}
