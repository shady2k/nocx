{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/vault.deleteSecret.schema.json",
  "title": "VaultDeleteSecret",
  "description": "Result of the vault.deleteSecret JSON-RPC method: the row (addressed by its opaque handle) and its stored material were deleted, metadata first (ADR-0011 §4). The renderer reloads the inventory to see the row gone.",
  "type": "object",
  "additionalProperties": false,
  "required": []
}
