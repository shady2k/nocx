{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/vault.replaceSecret.schema.json",
  "title": "VaultReplaceSecret",
  "description": "Result of the vault.replaceSecret JSON-RPC method: the user replaced a secret's value on the Secrets page. The reference did not change — the new material landed under the same SecretID — so every connection using the secret keeps working, and the name and kind are untouched. The renderer reloads the inventory to see the row.",
  "type": "object",
  "additionalProperties": false,
  "required": []
}
