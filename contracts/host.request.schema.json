{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/host.request.schema.json",
  "title": "HostRequest",
  "description": "Params of the host.request notification (nocx-uo1k6, design D3): the coordinator asks an attached client to perform one native-host capability it cannot perform itself. The coordinator is a daemon with no window (design §1), so a native file picker, a browser open, a desktop banner and a window raise are all reached by asking a client. requestId is the broker-minted correlation id the client echoes back in host.resolved; capability names which effect is asked for, from a closed server vocabulary. The remaining members are that capability's arguments and are absent for capabilities that take none: url for shell.openUrl; title, body and sessionId for attention.banner; count for attention.badge.",
  "type": "object",
  "additionalProperties": false,
  "required": ["requestId", "capability"],
  "properties": {
    "requestId": {
      "type": "string",
      "minLength": 1,
      "description": "The broker-minted request id; echoed back in host.resolved."
    },
    "capability": {
      "type": "string",
      "enum": [
        "dialog.file",
        "dialog.directory",
        "shell.openUrl",
        "attention.banner",
        "attention.badge",
        "attention.bounce",
        "window.focus"
      ],
      "description": "Which native-host effect the client is asked to perform."
    },
    "url": {
      "type": "string",
      "minLength": 1,
      "description": "shell.openUrl: the http(s) URL to open. Already validated by the transport."
    },
    "title": {
      "type": "string",
      "description": "attention.banner: the banner title, passed to the OS verbatim."
    },
    "body": {
      "type": "string",
      "description": "attention.banner: the banner body, passed to the OS verbatim."
    },
    "sessionId": {
      "type": "string",
      "description": "attention.banner: the session the banner is about, carried back on a click (AD-7 — session-id is server-authoritative)."
    },
    "count": {
      "type": "integer",
      "minimum": 0,
      "description": "attention.badge: the dock badge count; 0 clears it."
    }
  }
}
