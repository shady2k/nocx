{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/skills.install.params.schema.json",
  "title": "SkillsInstallParams",
  "description": "Params for skills.install: the address the person read and approved, and NOTHING else. There is deliberately no field carrying the body, the name or a digest — the backend fetches the address a second time and compares what comes back against the document its own preview showed, so a caller can select which approval it means but can never assert what the bytes were.",
  "type": "object",
  "additionalProperties": false,
  "required": ["url"],
  "properties": {
    "url": { "type": "string", "minLength": 1, "maxLength": 2048 }
  }
}
