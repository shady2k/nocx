{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/git.headMessage.params.schema.json",
  "title": "GitHeadMessageParams",
  "description": "Params for git.headMessage.",
  "type": "object",
  "additionalProperties": false,
  "required": ["bindingId"],
  "properties": { "bindingId": { "type": "string", "pattern": "^[0-9a-f]{32}$" } }
}
