{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/ledger.get.schema.json",
  "title": "LedgerGet",
  "description": "Result of the ledger.get JSON-RPC method (nocx-rtg0.20, design §6.2) — one entry with its relations and the METADATA of what its runs captured. The bodies are deliberately absent: the recall read must not haul bytes (ADR-0019 §6), so an artifact's content is fetched one artifact at a time by whoever actually wants it. The entry is the same shape a ledger.query page row has, declared once there.",
  "additionalProperties": false,
  "required": ["entry", "edges", "artifacts", "caused", "proseEvicted"],
  "properties": {
    "entry": { "$ref": "ledger.query.schema.json#/$defs/entry" },
    "edges": {
      "description": "Every edge touching this entry, in either direction — the difference between a log and a memory (design §3.4). Never null: no relation is [].",
      "type": "array",
      "items": { "$ref": "#/$defs/edge" }
    },
    "artifacts": {
      "description": "The metadata of every artifact of every execution of this entry, in execution order. Never null: no capture is [].",
      "type": "array",
      "items": { "$ref": "#/$defs/artifact" }
    },
    "proseEvicted": {
      "description": "Whether the prose of THIS RUN is no longer kept: retention took the bodies of its `text` children (ADR-0040's retention rule, ADR-0019 §7). It is the ONE place a reader asks that question, and it is a fact about the RUN because the run is the unit — the prose of one run is retained or evicted together, so a turn cut into seven pieces and a turn written in one report the same single answer, and the renderer drawing the turn has one sentence to say rather than one per hole. False on every kind that has no prose, which includes a command whose own terminal body was evicted — that block says its own sentence, and a turn does not say it for it.",
      "type": "boolean"
    },
    "caused": {
      "description": "Everything this entry caused, in the causal order the turn assigned (nocx-h1l4o) — the `caused-by` edges above, resolved. The join and the order are the ledger's: a reader that resolved raw edges itself would own the arrangement a second time (AD-8). Never null: an entry that caused nothing is [], which is also what a reader gets when the relation is missing, and it draws plain ledger order.",
      "type": "array",
      "items": { "$ref": "#/$defs/caused" }
    }
  },
  "$defs": {
    "edge": {
      "type": "object",
      "additionalProperties": false,
      "required": ["from", "to", "rel", "payload"],
      "properties": {
        "from": {
          "description": "The entry id the relation points from.",
          "type": "string"
        },
        "to": {
          "description": "The entry id the relation points to.",
          "type": "string"
        },
        "rel": {
          "description": "The edge vocabulary (design §3.4). Closed set, mirroring the store's CHECK constraint.",
          "enum": ["rerun-of", "supersedes", "caused-by", "cites", "in-span", "references"]
        },
        "payload": {
          "description": "The edge's sparse extension — for a `references` edge, the region inside the frame it points at. The store never interprets it; an edge that carries none is {}.",
          "type": "object"
        }
      }
    },
    "caused": {
      "description": "One CHILD of this entry, at its seat among its siblings (ADR-0040). There is deliberately no `at`: the offset said how much of one stored answer had been written when the cause happened, and it existed only while the unit that was DRAWN (a run of prose) and the unit that was STORED (the whole answer) were different things. They are the same thing now — prose is a `text` child with a seat of its own — so `position` IS the place and there is nothing left to cut. A command the turn ran is a block the page already carries and is placed by this; a tool call is an action entry that opened no block of its own and is drawn as a child naming its tool and its arguments; a run of prose is a `text` child whose body is fetched like any other.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "entryId",
        "position",
        "kind",
        "source",
        "intent",
        "args",
        "effect",
        "resource",
        "opensBlock"
      ],
      "properties": {
        "entryId": {
          "description": "The caused entry's id — the same id a page row carries for a command, and the address ledger.get takes.",
          "type": "string"
        },
        "position": {
          "description": "Where it sits inside the turn: a causal index the turn assigned, 0 for the first thing it caused. NOT a timestamp and NOT ingest_seq, which is commit order and never causality (ADR-0019 §2).",
          "type": "integer",
          "minimum": 0
        },
        "opensBlock": {
          "description": "Whether this cause's work became a TOP-LEVEL BLOCK of its own — the tool declaration's fact (internal/agenttools Declaration.OpensBlock), stored on the ACTION row with its attempt and read back here. True only for an action entry whose tool opens a block (`run`): the command's block, its output and its exit status are the account of that call, so the turn draws nothing beside it, and a second child would restate what the block already shows. False for every other action, whose own child is the only trace it left, and false for a shell entry — a command a turn ran IS a block and does not also say it opened one. Read rather than matched on `intent`, so a reader is never a second copy of the tool table.",
          "type": "boolean"
        },
        "kind": {
          "description": "What kind of entry it is. Closed set, mirroring the store's CHECK constraint — `text` included, because a run of assistant prose is a child like any other since ADR-0040 and the read returns it. `ask` is a TURN, which is never a child of a turn; the member is here because the enum mirrors the store.",
          "enum": ["shell", "ask", "action", "text"]
        },
        "source": {
          "description": "Who submitted the child's content or intent — the same entries.source fact a page row carries, so a restored turn's badge never guesses it from the child's kind.",
          "enum": ["user", "assistant"]
        },
        "intent": {
          "description": "The child's own intent: the command line for a shell entry, the declared tool name for an action, and EMPTY for a `text` child — prose has no intent, which is a clause of its CHECK rather than a convention a reader has to know.",
          "type": "string"
        },
        "args": {
          "description": "What the model asked for, as the tool's schema validated it, read back off the ACTION row's own record (content.ActionFacts). Null on every other kind \u2014 a command a turn ran is not a tool call and asked for nothing. It is here for the reason it is on agent.runToolCall: the arguments are what tell two calls of one tool apart, and a restored call naming only its tool and its derived resource would say LESS than the live one did \u2014 which is the defect ADR-0040 was written against, arriving one restart later. Stored rather than re-derived, like the effect and the resource beside it.",
          "oneOf": [
            {
              "type": "object",
              "additionalProperties": true
            },
            {
              "type": "null"
            }
          ]
        },
        "effect": {
          "description": "The effect class the gate decided for an ACTION entry, read back off that row's own record. Null on every other kind — a command a turn ran is not a tool call and has no effect class.",
          "oneOf": [
            {
              "enum": [
                "observe",
                "mutate-reversible",
                "mutate-destructive",
                "privilege-change",
                "disclose",
                "cross-boundary",
                "delegate"
              ]
            },
            { "type": "null" }
          ]
        },
        "resource": {
          "description": "What the call named, as the backend derived it at the moment it decided about the call — never re-derived by a reader. Null when the tool names no resource in its parameters at all, and null for a non-action entry.",
          "oneOf": [
            {
              "type": "object",
              "additionalProperties": false,
              "required": ["kind", "id"],
              "properties": {
                "kind": { "type": "string" },
                "id": { "type": "string" }
              }
            },
            { "type": "null" }
          ]
        }
      }
    },
    "artifact": {
      "description": "One capture of one execution, with the provenance ADR-0019 §6 requires: how the text was taken, at which version, from what geometry and stream position, and what is missing from it. There is no field for the content, and that absence is the contract.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "executionId",
        "mediaType",
        "derivedFrom",
        "state",
        "byteLen",
        "chunkCount",
        "pinned",
        "truncated",
        "captureMethod",
        "captureVersion",
        "terminalCols",
        "terminalRows",
        "stream",
        "byteOffset",
        "byteEnd",
        "encoding",
        "gaps",
        "payload"
      ],
      "properties": {
        "id": {
          "description": "The artifact's id — the address the content read takes.",
          "type": "string"
        },
        "executionId": {
          "description": "The run this artifact belongs to. Artifacts attach to the execution, never to the intent (ADR-0020 §4): a rerun and a retry are executions of one entry and each captured its own output.",
          "type": "integer"
        },
        "mediaType": {
          "description": "What the content is.",
          "enum": ["application/vt", "text/plain", "text/markdown", "application/json"]
        },
        "derivedFrom": {
          "description": "The artifact this one was derived from, or null when it is the original capture.",
          "type": ["string", "null"]
        },
        "state": {
          "description": "open while the capture may still grow, sealed once it cannot.",
          "enum": ["open", "sealed"]
        },
        "byteLen": {
          "description": "Logical content bytes — the retention budget's unit (§5.4), not the physical size on disk.",
          "type": "integer",
          "minimum": 0
        },
        "chunkCount": {
          "description": "How many chunks the content arrived in. An artifact is never one BLOB.",
          "type": "integer",
          "minimum": 0
        },
        "pinned": {
          "description": "Whether retention's background eviction is forbidden to take this artifact. A pin does not protect against an explicit delete.",
          "type": "boolean"
        },
        "truncated": {
          "description": "Why this artifact does not hold the whole stream: a cap dropped the middle, a gap lost a range, or capture was refused by policy. Null when nothing was lost.",
          "enum": ["cap", "gap", "suppressed", null]
        },
        "captureMethod": {
          "description": "Where the text came from — terminal cells, raw output, serialized block HTML, or nothing at all. Derived text must be able to say how it was taken.",
          "enum": ["terminal-cells", "raw-output", "serialized-html", "none"]
        },
        "captureVersion": {
          "description": "The version of the capture method, so text taken by an older serializer is not read as though it were today's.",
          "type": "integer",
          "minimum": 0
        },
        "terminalCols": {
          "description": "Terminal width at capture time, or null when the capture had no geometry.",
          "type": ["integer", "null"]
        },
        "terminalRows": {
          "description": "Terminal height at capture time, or null when the capture had no geometry.",
          "type": ["integer", "null"]
        },
        "stream": {
          "description": "Which stream was captured, or null when the capture does not distinguish them.",
          "enum": ["stdout", "stderr", "combined", null]
        },
        "byteOffset": {
          "description": "Where this capture starts in the run's byte stream, or null when it is not positioned in one.",
          "type": ["integer", "null"]
        },
        "byteEnd": {
          "description": "Where this capture ends in the run's byte stream, or null when it is not positioned in one.",
          "type": ["integer", "null"]
        },
        "encoding": {
          "description": "The content's encoding, so bytes are never decoded by guess.",
          "type": "string"
        },
        "gaps": {
          "description": "The dropped byte ranges of the captured stream. Never null: no gap is [].",
          "type": "array",
          "items": { "$ref": "#/$defs/gap" }
        },
        "payload": {
          "description": "The artifact's sparse extension. The store never interprets it; an artifact that carries none is {}.",
          "type": "object"
        }
      }
    },
    "gap": {
      "type": "object",
      "additionalProperties": false,
      "required": ["start", "end", "reason"],
      "properties": {
        "start": {
          "description": "First dropped byte offset.",
          "type": "integer",
          "minimum": 0
        },
        "end": {
          "description": "Offset just past the last dropped byte.",
          "type": "integer",
          "minimum": 0
        },
        "reason": {
          "description": "Why the range is missing.",
          "type": "string"
        }
      }
    }
  }
}
