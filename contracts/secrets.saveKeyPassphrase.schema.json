{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/secrets.saveKeyPassphrase.schema.json",
  "title": "SecretMintResult",
  "description": "Result of the secrets.saveKeyPassphrase JSON-RPC method: a key passphrase verified against the stored key and minted into the vault, addressed by the renderer's row handle.",
  "type": "object",
  "additionalProperties": false,
  "required": ["row"],
  "properties": {
    "row": {
      "description": "The renderer-addressable row handle of the minted secret (secrow:...). Never a secret reference — the renderer may not hold one (ADR-0011 §2).",
      "type": "string"
    }
  }
}
