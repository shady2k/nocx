# Fetched toolchain and upstream source: not evidence, not shipped. build.sh
# reconciles both to the pins in its header on every run.
.build/
.vendor/
# Zig's incremental cache, wherever the build leaves it.
.zig-cache/
zig-out/
zig-out-native/
# The full per-cell grid of every corpus replay: 1.2 MB per driver, and
# regenerated byte-for-byte by the two commands named in REPORT.md. The digest
# comparison of the same two runs IS committed, in results/corpus-compare.txt.
results/corpus-wasm.jsonl
results/corpus-native.jsonl
