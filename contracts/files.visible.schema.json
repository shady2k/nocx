{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/files.visible.schema.json",
  "title": "FilesVisibleResult",
  "description": "Result of the files.visible JSON-RPC method: an empty acknowledgement. The method's only observable effect is on the binding's poll cadence — no listing while hidden, one immediately on becoming visible.",
  "type": "object",
  "additionalProperties": false,
  "required": []
}
