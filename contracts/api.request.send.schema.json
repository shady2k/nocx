{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/api.request.send.schema.json",
  "title": "ApiRequestSendResult",
  "description": "Result of the api.request.send JSON-RPC method: what came back from one exchange, bounded. The body is captured behind the 2 MiB ceiling files.read already uses (design §12.3), so a response larger than that arrives truncated rather than not at all.",
  "type": "object",
  "additionalProperties": false,
  "required": ["response"],
  "properties": {
    "response": { "$ref": "#/$defs/response" }
  },
  "$defs": {
    "response": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "status",
        "headers",
        "text",
        "binary",
        "lossy",
        "truncated",
        "size",
        "timings",
        "tlsVersion",
        "remoteAddr",
        "raw"
      ],
      "properties": {
        "status": { "type": "integer" },
        "headers": {
          "description": "Never null — a response with no headers is [].",
          "type": "array",
          "items": {
            "$ref": "api.request.read.schema.json#/$defs/header"
          }
        },
        "text": {
          "description": "The decoded body, always valid UTF-8, and EMPTY when binary: the run says \"binary body, N bytes\" and never base64.",
          "type": "string"
        },
        "binary": {
          "description": "A NUL byte among the bytes actually read — the files.read heuristic, labelled as one.",
          "type": "boolean"
        },
        "lossy": {
          "description": "The bytes read were not valid UTF-8 and invalid sequences were replaced. Distinct from binary: a NUL-free latin-1 body is lossy text, not a binary body. Three separate facts because they are three separate sentences in the run, and collapsing any two of them loses one.",
          "type": "boolean"
        },
        "truncated": {
          "description": "The ceiling was reached and one further byte was readable — the body shown is a prefix.",
          "type": "boolean"
        },
        "size": {
          "description": "Body bytes actually read and kept, which is the ceiling when truncated. Not a claim about what the server holds: a Content-Length can lie and a chunked response declares nothing.",
          "type": "integer"
        },
        "timings": { "$ref": "#/$defs/timings" },
        "tlsVersion": { "type": "string" },
        "remoteAddr": { "type": "string" },
        "raw": { "$ref": "#/$defs/exchange" }
      }
    },
    "exchange": {
      "description": "The raw text of both sides, segmented (design §11). It rides on the send result rather than on a method of its own because the raw text belongs to a PARTICULAR run — this exchange, these substitutions, this truncation — so a second round trip could only fetch the raw of a different send. The two sides are two fields because they are two mechanisms with two different guarantees (§11.3): the request side verifies placements the sender itself made, the response side runs a bounded known-plaintext search.",
      "type": "object",
      "additionalProperties": false,
      "required": ["request", "response"],
      "properties": {
        "request": { "$ref": "#/$defs/raw" },
        "response": { "$ref": "#/$defs/raw" }
      }
    },
    "raw": {
      "description": "One side of the exchange, already segmented. A secret's VALUE never appears here in any state: the bytes are elided and a placeholder naming the secret takes their place, so a renderer that ignores spans entirely still shows no credential (§11.2, ADR-0011).",
      "type": "object",
      "additionalProperties": false,
      "required": ["text", "spans"],
      "properties": {
        "text": {
          "description": "The text as it crosses, AFTER elision. Bounded by the same 2 MiB ceiling as the captured body.",
          "type": "string"
        },
        "spans": {
          "description": "Never null — a side with nothing to mark is []. The spans tile the text in order with neither gap nor overlap, so a renderer draws the whole payload by walking them.",
          "type": "array",
          "items": { "$ref": "#/$defs/span" }
        }
      }
    },
    "span": {
      "description": "One run of the raw text. Three kinds, never two (§11.1): the bytes still equal the secret (secret, named — the badge is evidence rather than a curtain); our span but the bytes differ (secret-damaged, naming the SHAPE of the damage and never its bytes, because a truncated token is a prefix of a live one); or not our span at all (text).",
      "type": "object",
      "additionalProperties": false,
      "required": ["from", "to", "kind", "name", "damage"],
      "properties": {
        "from": {
          "description": "Byte offset into text where this run starts.",
          "type": "integer"
        },
        "to": { "description": "Byte offset into text where this run ends.", "type": "integer" },
        "kind": {
          "description": "Which of the three states this run is.",
          "type": "string",
          "enum": ["text", "secret", "secret-damaged"]
        },
        "name": {
          "description": "The NAME of the secret, never its value. Empty for a text run.",
          "type": "string"
        },
        "damage": {
          "description": "The shape of the damage — \"truncated, 24 of 214 bytes\" — carrying only lengths, because the surviving bytes are the beginning of a live credential. Empty unless the kind is secret-damaged.",
          "type": "string"
        }
      }
    },
    "timings": {
      "description": "The phases of one exchange, in milliseconds. On a redirect chain dns, connect and tls describe the LAST hop — the one the body came from — ttfb is measured from the start of the exchange, and total adds the body read. A reused connection has a zero dns and connect, which is the honest answer: nothing was resolved and nothing was dialled.",
      "type": "object",
      "additionalProperties": false,
      "required": ["dnsMs", "connectMs", "tlsMs", "ttfbMs", "totalMs"],
      "properties": {
        "dnsMs": { "type": "number" },
        "connectMs": { "type": "number" },
        "tlsMs": { "type": "number" },
        "ttfbMs": { "type": "number" },
        "totalMs": { "type": "number" }
      }
    }
  }
}
