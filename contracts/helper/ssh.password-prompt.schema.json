{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/helper/ssh.password-prompt.schema.json",
  "title": "HelperSSHPasswordPromptResult",
  "description": "The password a person typed, answering the coordinator's own connection-password ask relayed for the helper's interactive rung (nocx-y6fh7 item 4, round 3).",
  "type": "object",
  "additionalProperties": false,
  "required": ["password"],
  "properties": {
    "password": {
      "description": "The value to use for this connection's auth attempt. Secret material in transit, never logged, stored or returned by the helper.",
      "type": "string"
    }
  }
}
