{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/vault.setup.params.schema.json",
  "title": "VaultSetupParams",
  "description": "Params for vault.setup.",
  "type": "object",
  "additionalProperties": false,
  "required": ["passphrase"],
  "properties": {
    "passphrase": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64000,
      "description": "The vault's master passphrase. Required and non-empty: an empty one used to select a setup that put the root key in the OS keystore and minted no recovery code, and ADR-0050 removed that mode. Never log or echo it."
    }
  }
}
