{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/ledger.artifact.schema.json",
  "title": "LedgerArtifact",
  "description": "Result of the ledger.artifact JSON-RPC method (nocx-m3fqk, design \u00a76) \u2014 ONE artifact's body, joined from its chunks in seq order. It is a separate method from ledger.get on purpose: the recall read must not haul bytes (ADR-0019 \u00a76), so the page and the detail carry metadata and whoever actually wants a body asks for it one at a time. This is what a restored block's output is drawn from.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "mediaType", "body", "truncated", "byteLen"],
  "properties": {
    "id": {
      "description": "The artifact asked for, echoed back.",
      "type": "string"
    },
    "mediaType": {
      "description": "What the body is: application/vt carries the SGR a restored block draws, text/plain the derived text search and copy read, and application/x-nocx-rows is a streamed block's output \u2014 JSON Lines whose line shape ledger.blockRows.schema.json declares, one row of the screen frame's cell vocabulary per line.",
      "type": "string"
    },
    "body": {
      "description": "The chunks joined in seq order. Empty is a real answer and means the command printed nothing \u2014 it is NOT the same as an artifact that is not there, which is a body retention has evicted and which this method reports by refusing the id (ADR-0019 \u00a77).",
      "type": "string"
    },
    "truncated": {
      "description": "Why the body is not the whole of what was printed, or null when it is: 'cap' when the middle was dropped at the per-command limit, 'gap' when a range was lost, 'suppressed' when capture was refused.",
      "type": ["string", "null"],
      "enum": ["cap", "gap", "suppressed", null]
    },
    "byteLen": {
      "description": "The logical content bytes the store holds for this artifact \u2014 the retention budget's unit, and what a reader can check its join against.",
      "type": "integer",
      "minimum": 0
    }
  }
}
