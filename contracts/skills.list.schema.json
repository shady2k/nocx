{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/skills.list.schema.json",
  "title": "SkillsList",
  "description": "The discovered skills and their person-controlled state.",
  "type": "object",
  "additionalProperties": false,
  "required": ["skills", "documentPath"],
  "properties": {
    "skills": { "type": "array", "items": { "$ref": "#/$defs/skill" } },
    "documentPath": { "type": "string" },
    "documentError": { "type": "string" }
  },
  "$defs": {
    "skill": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "description", "provenance", "path", "enabled", "status"],
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" },
        "provenance": { "type": "string", "enum": ["authored", "builtin", "installed", "managed"] },
        "path": { "type": "string" },
        "enabled": { "type": "boolean" },
        "status": { "type": "string", "enum": ["approved", "changed"] },
        "source": {
          "description": "Where an installed skill came from, as skills.json recorded it at install time. ABSENT unless a source is recorded: never for authored, builtin or managed skills, and not for a directory somebody moved into the installed root by hand — so its presence answers where the bytes came from and never what provenance the skill has. Inlined rather than named, for the reason every finding here is: a named $def becomes a second generated export nothing consumes.",
          "type": "object",
          "additionalProperties": false,
          "required": ["url", "installedAt"],
          "properties": {
            "url": { "type": "string" },
            "installedAt": { "type": "string" }
          }
        }
      }
    }
  }
}
