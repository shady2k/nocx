{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/profiles.effective.schema.json",
  "title": "ProfilesEffectiveResponse",
  "description": "Result of the profiles.effective JSON-RPC method — the batched effective profile resolution with per-field provenance. The single declaration of this shape: the renderer's TypeScript type is generated from it and the Go transport is validated against it. The fields map is a closed set: propertyNames pins the exact key set ToEffectiveDTO emits, so a field dropped from the wire (or a renderer depending on one the wire does not carry) fails the contract instead of looking like a working field. The fields map carries the desiredMode axis (raw|script|helper); helperConsent is a required per-profile field beside id and fields because it is persisted per destination and never inherited.",
  "type": "object",
  "additionalProperties": false,
  "required": ["profiles"],
  "properties": {
    "profiles": {
      "description": "One resolved profile per requested id, in request order. Never null: no matches is [].",
      "type": "array",
      "items": {
        "$ref": "#/$defs/effectiveProfile"
      }
    },
    "errors": {
      "description": "Per-profile resolution failures, keyed by the requested id. Absent when every profile resolved. The renderer must not present a half-resolved batch as a whole one: a profile that errored appears here, not in profiles.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/profileError"
      }
    }
  },
  "$defs": {
    "effectiveProfile": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "fields", "helperConsent"],
      "properties": {
        "id": {
          "description": "The profile id this resolution belongs to.",
          "type": "string"
        },
        "fields": {
          "description": "Every inheritable option, resolved. Omission is indistinguishable from 'unresolved', so every field is always present — including zeros, false and the hardcoded default. The key set is closed: propertyNames pins the names ToEffectiveDTO emits and required demands all of them, so a field dropped from the wire fails the contract.",
          "type": "object",
          "required": [
            "host",
            "port",
            "passwordSecret",
            "keySecret",
            "keyPassphraseSecret",
            "user",
            "auth",
            "keepaliveInterval",
            "keepaliveCountMax",
            "readyTimeout",
            "jumpHost",
            "agentForward",
            "portDiscovery",
            "canBeJumpServer",
            "behaviorOnSessionEnd",
            "desiredMode"
          ],
          "propertyNames": {
            "enum": [
              "host",
              "port",
              "passwordSecret",
              "keySecret",
              "keyPassphraseSecret",
              "user",
              "auth",
              "keepaliveInterval",
              "keepaliveCountMax",
              "readyTimeout",
              "jumpHost",
              "agentForward",
              "portDiscovery",
              "canBeJumpServer",
              "behaviorOnSessionEnd",
              "desiredMode"
            ]
          },
          "additionalProperties": {
            "$ref": "#/$defs/effectiveField"
          }
        },
        "helperConsent": {
          "description": "The helper-consent axis (spec §3.5): whether the user has consented to nocx deploying the helper binary on this destination. Persisted per destination, never inherited; script mode never reads it. Always present — unknown is an honest state, never an absent field the renderer could silently read as granted.",
          "type": "string",
          "enum": ["unknown", "granted", "denied"]
        }
      }
    },
    "effectiveField": {
      "type": "object",
      "additionalProperties": false,
      "required": ["value", "source"],
      "properties": {
        "value": {
          "description": "The resolved value for this field. Type varies by field: string for host/user/secrets/enums, integer for ports and timeouts, boolean for toggles. tsType is a json-schema-to-typescript hint: the schema is deliberately untyped (the value really is field-dependent), and the generator renders an unrestricted schema as an index signature, which would reject scalar values at the type level.",
          "tsType": "unknown"
        },
        "source": {
          "$ref": "#/$defs/fieldSource"
        }
      }
    },
    "fieldSource": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "id", "label"],
      "properties": {
        "kind": {
          "description": "Closed enum for the renderer to switch on — never parse id/label to guess the layer.",
          "enum": ["profile", "group", "sshConfig", "global", "default"]
        },
        "id": {
          "description": "Group id when kind is group, empty otherwise.",
          "type": "string"
        },
        "label": {
          "description": "Group name when kind is group and the group is known, empty otherwise.",
          "type": "string"
        }
      }
    },
    "profileError": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "error"],
      "properties": {
        "id": {
          "type": "string"
        },
        "error": {
          "type": "string"
        }
      }
    }
  }
}
