package stanceless

import (
	"context"

	"github.com/shady2k/nocx/internal/credential"
)

func resolveWithoutStance(r credential.Resolver) {
	_, _ = r.Resolve(context.Background(), credential.SecretID("sec:v1:file:test"))
}
