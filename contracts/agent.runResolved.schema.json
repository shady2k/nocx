{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/agent.runResolved.schema.json",
  "title": "AgentRunResolved",
  "description": "Params of the agent.runResolved RPC (nocx-tjppv, design §4.1): the renderer's answer to an agent.runRequest — a closed outcome. outcome=completed carries the run body: the entry id the command was accepted under (minted at submit by the ordinary path), the exit status of the completed block (null when it froze without one — an entered environment; status names it honestly), the block's output line count and the span of the window actually returned with its text (a long output is clamped, never truncated silently — total says how much more the block holds). outcome=failed carries why, so a renderer that cannot submit (a session it does not know, a lane not prompt-ready) answers honestly instead of hanging the run. requestId is the broker-minted id from the request, echoed back.",
  "type": "object",
  "additionalProperties": false,
  "required": ["requestId", "outcome"],
  "properties": {
    "requestId": {
      "type": "string",
      "minLength": 1,
      "description": "The request id from agent.runRequest."
    },
    "outcome": {
      "type": "string",
      "enum": ["completed", "failed"],
      "description": "completed: the command completed and the run body follows. failed: the submission could not be made or completed and error names why."
    },
    "error": {
      "type": "string",
      "description": "The failure sentence, present only when outcome is failed."
    },
    "entryId": {
      "type": "string",
      "minLength": 1,
      "description": "The entry id the command was accepted under, minted at submit by the ordinary path."
    },
    "exitCode": {
      "type": ["integer", "null"],
      "description": "The exit status of the completed block; null when it froze without one (an entered environment)."
    },
    "status": {
      "type": "string",
      "enum": ["success", "failure", "entered", "unknown"],
      "description": "The block's own frozen status vocabulary."
    },
    "total": {
      "type": "integer",
      "minimum": 0,
      "description": "The block's output line count — how much output the command produced in total."
    },
    "start": {
      "type": "integer",
      "minimum": 0,
      "description": "First line of the returned window, inclusive."
    },
    "end": {
      "type": "integer",
      "description": "Last line of the returned window, exclusive; within [0, total]."
    },
    "text": {
      "type": "string",
      "description": "The returned window's text."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "outcome": { "const": "completed" } },
        "required": ["outcome"]
      },
      "then": {
        "required": ["entryId", "status", "total", "start", "end", "text"]
      }
    },
    {
      "if": {
        "properties": { "outcome": { "const": "failed" } },
        "required": ["outcome"]
      },
      "then": {
        "required": ["error"]
      }
    }
  ]
}
