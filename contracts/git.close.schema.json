{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/git.close.schema.json",
  "title": "GitCloseResult",
  "description": "Result of the git.close JSON-RPC method: the binding is closed — removed from the registry and drained — so the repository it guarded is released. closed is always true in a result; a close of an unknown binding answers the unknownBinding error instead (the store re-resolves on the one reason \"unknown-binding\", nocx-bpqil).",
  "type": "object",
  "additionalProperties": false,
  "required": ["closed"],
  "properties": {
    "closed": {
      "type": "boolean"
    }
  }
}
