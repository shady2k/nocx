{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nocx.local/contracts/vault.renameSecret.params.schema.json",
  "title": "VaultRenameSecretParams",
  "description": "Params for vault.renameSecret.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "name"],
  "properties": {
    "id": { "type": "string", "pattern": "^secrow:[0-9a-f]{32}$" },
    "name": { "type": "string", "minLength": 1, "maxLength": 200 }
  }
}
